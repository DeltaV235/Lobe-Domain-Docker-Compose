# Cloudflare API 凭证配置
# 用于 Certbot DNS-01 验证

# ==================== 方式 1: API Token (强烈推荐) ====================
# 优势：更安全，权限可精细控制，易于撤销

# 📌 创建步骤：
# 1. 登录 Cloudflare Dashboard: https://dash.cloudflare.com/profile/api-tokens
# 2. 点击 "Create Token"
# 3. 使用 "Edit zone DNS" 模板（或自定义）

# 🔐 权限设置（根据官方文档要求）：
# 必须权限：
#   - Zone > DNS > Edit           （必须：允许创建/删除 DNS TXT 记录用于验证）
#
# 推荐权限（避免权限不足错误）：
#   - Zone > Zone > Read          （推荐：允许列出和读取区域信息）
#
# 📂 区域资源（Zone Resources）：
#   方式A（推荐）：Include > All zones
#           - 适用于管理多个域名的证书
#           - 更灵活，新增域名无需更新 token
#
#   方式B：Include > Specific zone > 选择特定域名（如 deltav235.com）
#           - 适用于单一域名或严格权限控制
#           - 更安全，但每个新域名需要新的 token 或更新权限
#
# 5. 复制生成的 token 并替换下面的 YOUR_CLOUDFLARE_API_TOKEN

dns_cloudflare_api_token =


# ==================== 方式 2: Global API Key (不推荐) ====================
# ⚠️ 警告：此方式权限过大，可访问账户所有 API，存在严重安全风险
# 仅在以下情况使用：
#   - cloudflare Python 模块版本 < 2.3.1（无法使用 API Token）
#   - 特殊兼容性需求
#
# 如果必须使用，请：
#   1. 注释掉上面的 dns_cloudflare_api_token 行
#   2. 取消下面两行的注释并填入真实值
#   3. API Key 获取路径：Dashboard > My Profile > API Tokens > API Keys > Global API Key > View

# dns_cloudflare_email = your-email@example.com
# dns_cloudflare_api_key = YOUR_GLOBAL_API_KEY


# ==================== 重要提示 ====================
# 1. 🔑 请将 YOUR_CLOUDFLARE_API_TOKEN 替换为你的真实 token
# 2. 🔒 此文件包含敏感信息，已通过 chmod 600 设置为仅所有者可读
# 3. 🚫 请勿将此文件提交到 Git 仓库（已添加到 .gitignore）
# 4. 📝 Token 创建后请记录其用途和创建时间，便于后续管理
# 5. 🔄 定期轮换 API Token 以提高安全性

# ==================== 参考文档 ====================
# certbot-dns-cloudflare 官方文档：
#   https://certbot-dns-cloudflare.readthedocs.io/
# Cloudflare API Token 创建指南：
#   https://developers.cloudflare.com/fundamentals/api/get-started/create-token/

